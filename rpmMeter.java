public class rpmMeter {
    // Make a new rpm meter to display the rpm in the same way like semiCircularSpeedometer.java
    // Always pass the rpm value to the constructor
    int rpm;
    public rpmMeter() {}
}
